# Normative crosswalk

**Informative.** This document maps every RFC 2119 statement in `spec/trace-v0.2.md`
to two things the statement itself does not say: **whom it binds**, and **what, if
anything, enforces it**. It changes no normative text.

The first column exists because it is the easy mistake. A requirement that binds a
gateway cannot and need not be enforced by a verifier's schema; one that binds a profile
*document* is discharged by a sentence in that document, not by code. Reading every
requirement as "the SDK must check this" produces findings that dissolve on contact with
the addressee, an error this repository has made and recorded.

The uppercase keywords in the rows below are quotations from `spec/trace-v0.2.md`, cited
by section. This prose uses none of its own: a keyword here would be a requirement no
specification contains, in a file that binds nothing.

**Maintenance contract.** The statement inventory is recovered from the spec source by
`tests/test_normative_crosswalk.py`; the mapping is maintained by hand and guarded. A
normative keyword added to the spec without a row here fails the test, as does a row
whose quoted text no longer appears in the spec. The quotes are verbatim, so the guard
is exact.

**Scope.** MAY and OPTIONAL are excluded: they grant permissions, and a permission has
no enforcement point. Every RFC 2119 requirement keyword
outside a code fence is in the table.

## The map

| Spec text (verbatim) | Binds | Enforced by | Notes |
|---|---|---|---|
| A v0.2 verifier MUST require | Verifier | `verify_record` defaults to the v0.2 profile alone and refuses a configuration containing the v0.1 identifier; vectors 03 and 08 in `examples/verifier-compatibility/`; `test_sign.py` cutover tests | Covers all three obligations of the cutover paragraph: require v0.2, reject v0.1, never both. The dual-accept configuration is unrepresentable in this library. No cutover vector exists in upstream `trace-tests`. Stated under *Changes from v0.1* in `spec/trace-v0.2.md`, a heading that carries no number. |
| A record whose `origin.kind` is not `self` MUST carry | Producer **and** verifier | `TrustRecord._origin_cannot_claim_hardware` (cross-field validator) and the `allOf` `if`/`then` in both schema copies | The only obligation in this spec binding both sides of the same sentence, and the reason it holds: a rule enforced only in `build_record` is a rule an attacker never runs. Upstream #135. Tested from both directions, and the schema half means a validator that never loads the Python rejects it too. §3.1.1. |
| `declared` is the weakest value and MUST NOT be a default | Producer, then consumer | Partly nothing mechanical: a default is producer behaviour and invisible in the record produced. The model requires `enforcement_mode` explicitly, with no default, which satisfies the MUST NOT structurally | Upstream #143. The consumer half (MUST NOT read `declared` as evidence any rule was checked) is not mechanically enforceable either: it constrains what a relying party concludes, not what any record contains. Same shape as the `enforce` default two rows up. §4.3. |
| `enforcement_mode` MUST default to `enforce` | Gateway (producer) | Nothing mechanical — a default is producer behaviour, invisible in the record it produces | JSON Schema `default` is an annotation, not a constraint, so the schema cannot carry this. This SDK's model takes a third position: the field is required with no default, so a producer must choose explicitly. That satisfies the MUST NOT below structurally and leaves this MUST to gateway implementations. §3.1. |
| Workload-level keys SHOULD rotate at TEE-image boundaries | Deployment | Nothing — rotation policy is not observable in a single record | A relying party can observe rotation only across records over time. §3.2.1. |
| `references` MUST NOT affect `runtime.platform` | Verifier (appraisal) | Structural: nothing derives a platform value from `references`, and `Reference` carries no path to `RuntimeInfo`. A record with `references` and no `origin` stays `self` | The obligation is that an assurance-neutral block stay neutral, so it is discharged by an absence rather than by a check. Before the block existed, naming something external forced `origin` and `software-only` with it, which said something untrue about how the evidence was obtained. §3.1.2. |
| The record signature MUST cover `references` | Producer and verifier | `sign_record` / `verify_record` canonicalise the whole record under RFC 8785, so a record field is covered by construction rather than by enumeration | What the signature attests is that this record points there, not the truth of what it points at. §3.1.2, over the canonicalisation in §3.2.2. |
| A verifier MUST NOT reject a record because an entry in `references` cannot be resolved | Verifier | Nothing mechanical, and the schema says so in its own description: both prohibitions on this line bind verifier behaviour and are not expressible in a record shape | A prohibition on an action has no enforcement point in a validator; what can be checked is that a conformant verifier does not take it, which is a conformance-suite question. §3.1.2. |
| A producer that cannot name a `resolver` MUST omit the entry | Producer | `Reference.resolver` is required in the model and in `required` in both schema copies, so an entry without one does not validate | Presence only. Whether an identifier is self-asserted is not decidable from the record, so the constraint enforced is narrower than the one stated, and the gap is in the field's docstring rather than implied. §3.1.2. |
| A record whose entry ID is greater MUST be rejected | Verifier | Nothing. `verify_record(revocation=...)` compares key identifiers and has no concept of an entry ID or a log | The SCITT-anchored rule is the primary one and this library does not implement it: there is no `last_valid_entry_id`, no `log_id`, and no receipt comparison anywhere in `src/`. What ships is the fallback in the next row, which §3.2.3 itself names as the behaviour for deployments carrying no receipts. Incomplete rather than wrong, and the distinction is the whole row. §3.2.3. |
| a verifier MUST reject every record signed by the revoked key | Verifier | `verify_record(revocation=...)` via `_check_not_revoked`: a match on the RFC 7638 thumbprint or the `kid` rejects, and a store that raises is treated as a rejection | This is the fallback, and it is what this library does in every case rather than only where a receipt is absent. Fail-closed on an indeterminate store, because a store that cannot answer has not said the key is good. §3.2.3. |
| MUST be signed by a key at a higher level | Issuer of the revocation statement (organisational) | Nothing mechanical, and nothing here could: the hierarchy and the recovery key are deployment facts, not record fields | Binds whoever issues the statement, not the verifier reading it. A statement a key could sign for itself would let a compromised key name its own `last_valid_entry_id`, which is why the rule exists; discharged by key management rather than by code. §3.2.3. |
| MUST report the record as unverified for revocation | Verifier | Half. `VerificationStatement.revocation_checked` is exactly the second obligation on this line, that a verifier with no bundle report performing no check: `False` means non-revocation is unproven, not disproven. The bundle-age half is unimplemented, since no bundle exists to age | A default offline run does not consult and says so, which is the pattern this row and `LIMITATIONS.md` both describe: make the gap visible instead of silent. Neither state may be reported as an affirming appraisal, and neither is. §3.2.3. |
| MUST be cryptographically bound by a signature | Producer and verifier | `sign_record` / `verify_record`; every fixture in the repository is genuinely signed and independently re-verified by `test_fixture_signatures_independent.py` | §3.2.2. |
| MUST use an RFC 8785-conformant library | Implementation (both sides) | Upstream `test_sign.py` has four literal-byte known-answer tests over `_canonical_bytes`, which do catch a regression in this library. `examples/canonicalization-boundary/` adds portable material: three schema-valid signed records another implementation can run. | **Correction, 2026-08-07:** an earlier revision of this row said nothing upstream caught a `json.dumps` swap. That was wrong and was asserted without reading `test_sign.py`. Two narrower gaps survive: the unit tests are not runnable by another implementation (v1.0 targets Go, Rust and TypeScript verifiers), and `test_jcs_distinguishes_unicode_key_order_from_json_dumps` cannot catch a code-point sorter — its example object sorts identically under both schemes, as its own docstring notes, so it detects the divergence through `ensure_ascii` escaping instead. The IEEE 754 half is separately unreachable inside a schema-valid record: no field is typed `number`, and a test pins that. §3.2.2. |
| Each profile MUST declare which binding form it uses | Profile author (document) | The declaration lives in profile prose — the normal EAT-profile pattern, not a record field | Partially discharged for v0.2 itself: the field table describes both forms and the reference verifier accepts only the embedded one, but no sentence in the profile text says "this profile uses the embedded form". One sentence would close it. §3.2.2. |
| verifiers MUST reject it | Verifier | `verify_record` refuses a record with no `signature` and requires a trusted key; upstream `trace-tests` fails a missing signature at conformance level 1 and above (TR-SIG-005) | "It" is a record with no verifiable signature binding. §3.2.2. |
| Records MUST carry `iat` | Producer | `iat` is in the schema's `required` array; `validate_json` rejects its absence | §3.2.2. |
| Verifiers MUST enforce a maximum record age | Verifier | `verify_record` `max_age_seconds` (default 86400); upstream `trace-tests` fails a 25-hour-old record at every level (TR-ENV-002) | Passing `None` disables the check — a caller decision the statement records via `freshness_checked`. §3.2.2. |
| SHOULD additionally support challenge-nonce binding | Verifier | `verify_record(expected_nonce=...)`; `test_verify_record_nonce_match` | §3.2.2. |
| a record that omits or mismatches it MUST be rejected | Verifier | Same code path: mismatch raises, comparison is constant-time | Applies when a challenge nonce was issued. §3.2.2. |
| A record with no verifiable binding MUST be rejected | Verifier | `verify_record` (no signature, no trusted key, or bad signature all raise); the same sentence's ordering clause — "BEFORE any other field is trusted" — is partially operationalizable at best | "Trusted" is undefined, and cannot be fully literal: `cnf` must be read before verification to obtain the key, and this implementation reads `eat_profile` first to refuse unimplemented semantics. The workable reading — no field may be *relied on for anything but refusal* before the signature verifies — is what the implementation does. A one-line clarification in the spec would make the ordering requirement checkable. §3.3. |
| MUST treat the audit entry as invalid | Verifier | Action-receipt fixtures 04, 05, 07 and 17–21: signature mismatch, binding mismatch, and chain-gap failures all yield `receipt_invalid` | The configured-key case: any of the three checks failing is positive evidence. §3.3.2. |
| SHOULD surface an advisory status | Verifier | Fixtures 16 and 22: unknown issuer key yields `gap_disclosure_unverified` / `receipt_unverified` with a `*_key_unknown` advisory and no failures | These fixtures contradicted this sentence until 2026-08-07 — they returned `receipt_invalid`. The correction is recorded in `DECISIONS.md`. §3.3.2. |
| Gateways MUST default `enforcement_mode` to `enforce` | Gateway (producer) | Nothing mechanical (same obligation as the claim-table row above) | §4.3. |
| A deployment MUST explicitly configure `silent` mode | Deployment | Not observable in a record; the record shows only which mode was in force | An auditor checks this against deployment configuration, not against a Trust Record. §4.3. |
| `silent` MUST NOT be the default | Gateway (producer) | Structurally, in this SDK: the model has no default at all, so `silent` cannot be one | §4.3. |
| A verifier MUST record the depth it actually checked in | Verifier | `appraisal.provenance_depth_verified` exists in the model and both schema copies; `examples/build-provenance-depth/` (6 vectors) and `tests/test_build_provenance_depth_vectors.py` assert a `verified_depth` per vector at every depth | The record carries the depth claimed and the depth checked as separate fields, so a relying party can see the gap rather than infer it. §3.3.1. |
| a record that omits `provenance_depth` MUST be treated as `surface` | Verifier | Nothing mechanical. The field is optional in both schema copies, and the default is a reading rule applied by a verifier, not a value present in the record | A schema `default` is an annotation and not a constraint, so absence cannot be distinguished from an explicit `surface` after the fact. §3.3.1. |
| At `surface`, the verifier MUST confirm that `digest` matches the independently held workload | Verifier | Not in this SDK: the artifact is held outside the record. `examples/build-provenance-depth/` (6 vectors) and `tests/test_build_provenance_depth_vectors.py` pin the boundary's verdicts, not the fetch | The comparison needs an artifact this library never sees. A vector can say what the verdict must be and cannot supply the input. §3.3.1. |
| artifact and that `builder` belongs to its configured trusted-builder set. At `builder`, it MUST | Verifier | `examples/build-provenance-depth/` (6 vectors) and `tests/test_build_provenance_depth_vectors.py`: vectors 02 and 03 separate the `builder` boundary on attestation subject and builder identity | Two vectors on distinct defects, which is the margin rule of upstream #124. §3.3.1. |
| attestation subject and builder identity match the record. At `transitive`, it MUST additionally | Verifier | `examples/build-provenance-depth/` (6 vectors) and `tests/test_build_provenance_depth_vectors.py`: vectors 04, 05 and 06 separate the `transitive` boundary | Upstream #169 established that two vectors sharing a positional weakness do not separate a boundary; 04 and 05 were reordered for that reason. §3.3.1. |
| resolved, the verifier MAY stop at the preceding depth. It MUST record that lower verified depth | Verifier | `examples/build-provenance-depth/` (6 vectors) and `tests/test_build_provenance_depth_vectors.py`: 04 and 06 accept at `transitive` with `verified_depth` short of it and the reason under `unresolved` | The permission and the obligation are one sentence: stopping is allowed, stopping silently is not. §3.3.1. |
| and identify the unresolved evidence. It MUST NOT record a depth higher than it executed. There | Verifier | Nothing mechanical. No structure prevents a producer writing a depth it did not execute, and the record cannot show what a verifier did | The same shape as `enforcement_mode` defaults: a claim about the issuer's own conduct, invisible in the artefact it produces. §3.3.1. |
| When evidence resolves and contradicts the record, the verifier MUST fail the appraisal and MUST | Verifier | `examples/build-provenance-depth/` (6 vectors) and `tests/test_build_provenance_depth_vectors.py`: vectors 02 and 03 reject rather than downgrade when the attestation contradicts the record | This is the sentence that makes downgrade safe: unresolved evidence lowers the depth, contradicted evidence fails. §3.3.1. |
| `appraisal.provenance_depth_verified` is below that floor, the verifier MUST set | Deployment profile, then verifier | Nothing mechanical here: no profile in this repository sets a floor, so no vector can exercise the comparison | Enforceable only once a profile document names a floor. Until then the obligation has no configured input. §3.3.1. |

## What this establishes, and does not

- **A row saying "nothing mechanical" is not an accusation.** Several statements bind
  producers or deployments, whose behaviour is not observable in the artifacts this
  repository holds. The row records where enforcement would have to live, which is not
  here.
- **Enforcement in this repository is not enforcement everywhere.** Rows citing
  `verify_record` or local vectors describe the reference implementation. Another
  implementation meets the same requirement with its own machinery — the portable vectors
  exist so that it can be checked, and the upstream `trace-tests` citations record
  which of these obligations the normative suite exercises today.
- **Two rows describe spec text that could be sharpened** (binding-form declaration,
  "before any other field is trusted"). Both are one-sentence clarifications. They are
  noted here and not drafted: normative edits need a sponsor and a comment window
  under `GOVERNANCE.md`.
